import * as React from 'react'
import { useEffect, useRef, useState } from 'react'
import type { AnalysisStageId } from '../analysis'
import type { AgentStatusDto, ModelDto, ProviderDto } from '../local-api/contracts'
import type { Accent, Appearance } from './ThemeMenu'
import { ThemeMenu } from './ThemeMenu'
import { useControlMotion } from '../motion/useControlMotion'
import { useEntranceMotion } from '../motion/useEntranceMotion'

interface LauncherProps {
  agent?: AgentStatusDto; models: ModelDto[]; providers?: ProviderDto[]; runtimeStatus: string; isAnalysing: boolean; analysisStage?: AnalysisStageId; error?: string
  appearance: Appearance; accent: Accent; onAppearance: (value: Appearance) => void; onAccent: (value: Accent) => void
  onTrySample: (modelId: string) => void; onUsePrepared: () => void; onCancel: () => void; onRefreshProviders?: () => void; onRefreshModels?: () => void
  onConnectProvider?: (providerId: string) => void; onDisconnectProvider?: (providerId: string) => void
}

export function Launcher({ agent, models = [], providers = [], runtimeStatus = 'Checking local runtime…', isAnalysing, analysisStage, error, appearance, accent, onAppearance, onAccent, onTrySample, onUsePrepared, onCancel, onRefreshProviders = () => {}, onRefreshModels = () => {}, onConnectProvider = () => {}, onDisconnectProvider = () => {} }: LauncherProps) {
  const motionScope = useRef<HTMLElement>(null)
  const [model, setModel] = useState(() => typeof localStorage === 'undefined' ? '' : localStorage.getItem('project-lens-model') ?? '')
  const [query, setQuery] = useState(''); const [freeOnly, setFreeOnly] = useState(false); const [pickerOpen, setPickerOpen] = useState(false); const [activeIndex, setActiveIndex] = useState(0)
  const selectedModel = models.some((item) => item.fullId === model) ? model : ''
  const selectedData = models.find((item) => item.fullId === selectedModel)
  const visibleModels = models.filter((item) => (!query || `${item.providerId} ${item.fullId}`.toLowerCase().includes(query.toLowerCase())) && (!freeOnly || item.free === true))
  useEffect(() => { if (selectedModel) localStorage.setItem('project-lens-model', selectedModel); else localStorage.removeItem('project-lens-model') }, [selectedModel])
  useEntranceMotion(motionScope, isAnalysing ? 'analysing' : 'launcher'); useControlMotion(motionScope)
  function chooseModel(id: string) { setModel(id); setPickerOpen(false); setQuery('') }
  function handleModelKey(event: React.KeyboardEvent<HTMLInputElement>) { if (event.key === 'Escape') setPickerOpen(false); if (event.key === 'ArrowDown') { event.preventDefault(); setActiveIndex((index) => Math.min(index + 1, visibleModels.length - 1)) } if (event.key === 'ArrowUp') { event.preventDefault(); setActiveIndex((index) => Math.max(index - 1, 0)) } if (event.key === 'Enter' && visibleModels[activeIndex]) chooseModel(visibleModels[activeIndex].fullId) }
  return <main className="launcher-screen" ref={motionScope}>
    <header className="launcher-header"><div className="brand-group"><span className="brand-mark" aria-hidden="true"><svg viewBox="0 0 16 16" fill="none"><path d="M8 1.5l5 2.8v7.4L8 14.5 3 11.7V4.3l5-2.8Z" stroke="currentColor" strokeWidth="1.4" strokeLinejoin="round" /><path d="M8 4.2v7.6M5 6.4l3 1.7 3-1.7" stroke="currentColor" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round" /></svg></span><strong className="brand">Project Lens</strong></div><ThemeMenu appearance={appearance} accent={accent} onAppearance={onAppearance} onAccent={onAccent} /></header>
    <section className="launcher-stage"><div className="launcher-copy-block" data-motion-enter><h1>Understand what was built.</h1><p className="launcher-copy">A calm workspace for opening a project, inspecting what matters, and learning from the implementation.</p></div>
      <div className="lens-launcher" aria-label="Prepared sample launcher" data-motion-enter><button className="launcher-folder" disabled type="button"><span className="launcher-folder-icon" aria-hidden="true"><svg viewBox="0 0 16 16" fill="none"><path d="M1.5 4.5h4l1.3 1.6h7.7v5.9a1 1 0 0 1-1 1H2.5a1 1 0 0 1-1-1v-7.5a1 1 0 0 1 1-1Z" stroke="currentColor" strokeWidth="1.2" strokeLinejoin="round" /></svg></span><span className="folder-label">Choose a project folder</span></button><div className="launcher-divider" /><div className="launcher-controls">
        <div className="launcher-picker"><button className="model-trigger" type="button" role="combobox" aria-expanded={pickerOpen} aria-controls="model-listbox" onClick={() => setPickerOpen((open) => !open)}><span className="picker-summary-icon" aria-hidden="true"><svg viewBox="0 0 16 16" fill="none"><path d="M3 4.5h10M3 8h10M3 11.5h10" stroke="currentColor" strokeWidth="1.3" strokeLinecap="round" /></svg></span><span id="launcherPickerValue">{selectedData?.displayName ?? (agent?.installed ? 'Choose a model' : 'OpenCode unavailable')}</span></button>{pickerOpen && <div className="launcher-picker-menu" role="dialog" aria-label="Choose model"><div className="launcher-picker-field"><label htmlFor="modelSearch">Search models</label><input id="modelSearch" autoFocus type="search" placeholder="Provider or model" value={query} onChange={(event) => { setQuery(event.target.value); setActiveIndex(0) }} onKeyDown={handleModelKey} /><label className="launcher-filter"><input type="checkbox" checked={freeOnly} onChange={(event) => setFreeOnly(event.target.checked)} /> Free IDs only</label><div id="model-listbox" role="listbox" aria-label="Available OpenCode models">{visibleModels.map((item, index) => <button key={item.fullId} className={`model-option${item.fullId === selectedModel ? ' selected' : ''}${index === activeIndex ? ' active' : ''}`} role="option" aria-selected={item.fullId === selectedModel} title={item.fullId} type="button" onMouseDown={(event) => event.preventDefault()} onClick={() => chooseModel(item.fullId)}><strong>{item.displayName}</strong><span>{item.providerId}{item.free ? ' · Free' : ''}{item.local ? ' · Local' : ''}</span></button>)}{!visibleModels.length && <p className="model-empty">{models.length ? 'No models match. Clear the search or filter.' : 'No models available. Refresh models or connect a provider.'}</p>}</div></div></div>}</div>
        <button className="launcher-analyse" disabled={isAnalysing || !agent?.installed || !selectedModel} type="button" onClick={() => onTrySample(selectedModel)}>{isAnalysing ? 'Analysing…' : 'Analyse sample'}</button></div></div>
      <div className="launcher-footer" data-motion-enter><button className="link-sample" disabled={isAnalysing} type="button" onClick={onUsePrepared}>Use prepared sample <span aria-hidden="true">→</span></button><span className="launcher-status">{runtimeStatus}</span></div>
      <details className="launcher-settings"><summary>Settings · AI providers</summary><div className="launcher-settings-body"><p>OpenCode owns provider credentials. Project Lens never collects or stores API keys.</p>{providers.map((provider) => <div className="provider-row" key={provider.id}><span>{provider.displayName} · {provider.connected ? 'Connected' : 'Not connected'}</span>{provider.connected ? <button type="button" onClick={() => onDisconnectProvider(provider.id)}>Disconnect</button> : <button type="button" onClick={() => onConnectProvider(provider.id)}>Connect through OpenCode</button>}</div>)}<div className="provider-actions"><button type="button" onClick={onRefreshProviders}>Refresh providers</button><button type="button" onClick={onRefreshModels}>Refresh models</button></div></div></details>
      {error && <p className="error-state" role="alert">{error}</p>}{isAnalysing && <p className="analysis-activity" role="status"><span aria-hidden="true" />{analysisStage ? `Checking ${analysisStage}…` : 'Analysing with OpenCode…'} <button type="button" className="link-sample" onClick={onCancel}>Cancel</button></p>}
    </section></main>
}
