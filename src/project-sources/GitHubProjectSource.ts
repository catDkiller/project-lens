import type { ComingSoonProjectSource } from './types'

export const gitHubProjectSource: ComingSoonProjectSource = {
  id: 'github', kind: 'github', label: 'GitHub repository', status: 'coming-soon',
}
