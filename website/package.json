{
  "name": "project-lens-demo-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "tsc -p tsconfig.node.json --noEmit",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.1.10"
  }
}
