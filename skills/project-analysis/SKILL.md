# Project Lens analysis

Analyse only the project snapshot in `source/`. Do not access paths outside the run directory.

Write these Markdown artifacts under `artifacts/`:

- `overview.md`: a concise evidence-based description of the project, its likely purpose, technologies, entry points, architecture, important directories/files, and uncertainty.
- `complete-guide.md`: a practical reading order, project structure, how major parts connect, dependencies/configuration, tests/workflows, concepts to learn, and limitations.

Use only files present under `source/`. Use relative source paths when citing files. Do not invent files, technologies, or behavior. State uncertainty when evidence is incomplete. Verify both artifacts are non-empty before finishing.

